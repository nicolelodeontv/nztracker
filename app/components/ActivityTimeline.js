const fmt = (n) => Number(n || 0).toLocaleString();
export default function ActivityTimeline({ events = [] }) { return <section className="nz-card"><div className="nz-card-head"><h3>Activity Timeline</h3><span>24H</span></div><div className="nz-log">{events.slice(0, 8).map((e) => <div className="nz-list-row" key={`${e.t}-${e.member}`}><span>{new Date(e.t).toLocaleTimeString('en-PH',{hour:'2-digit',minute:'2-digit'})}</span><b>{e.member}</b><strong>+{fmt(e.gain)}</strong></div>)}{!events.length && <div className="nz-empty small">No recent gain events.</div>}</div></section>; }
