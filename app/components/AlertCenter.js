export default function AlertCenter({ alerts = [] }) { return <section className="nz-card"><div className="nz-card-head"><h3>Alert Center</h3><span>{alerts.length}</span></div><div className="nz-log">{alerts.slice(0,8).map((a, i)=><div className={`nz-alert ${String(a.level).toLowerCase()}`} key={`${a.type}-${i}`}><strong>{a.type}</strong><span>{a.text}</span></div>)}{!alerts.length && <div className="nz-empty small">No active alerts.</div>}</div></section>; }
