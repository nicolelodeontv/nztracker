export function buildRecentActivityEvents(points, limit=12){const pending=new Map(),events=[];for(const point of points||[]){const key=String(point.member_id);const older=point;const newer=pending.get(key);if(newer&&Number(newer.reputation)>Number(older.reputation)){events.push({memberId:key,member:newer.ign,gain:Number(newer.reputation)-Number(older.reputation),at:newer.captured_at});if(events.length>=limit)break;}pending.set(key,older);}return events;}
